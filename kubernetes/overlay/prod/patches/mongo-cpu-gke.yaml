# Right-size CPU for GKE scheduling (base uses higher requests/limits).
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo-shards
spec:
  template:
    spec:
      containers:
        - name: mongo
          resources:
            requests:
              cpu: 100m
            limits:
              cpu: 500m
